#bootstrap properties file loads before application.properties

spring.profiles.active=development
spring.application.name=config-client

#have to manually enable specific actuator endpoints when using Spring Boot 2
management.endpoints.web.exposure.include=refresh,health,info,features
        
#configuration server uri
spring.cloud.config.uri=http://localhost:8888